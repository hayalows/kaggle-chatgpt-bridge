# Generate this in Kaggle Settings > API > Generate New Token
KAGGLE_API_TOKEN=replace_me

# Generate your own random secret, for example: openssl rand -hex 32
BRIDGE_API_KEY=replace_with_a_long_random_secret

# Set this after Vercel gives you the production URL
PUBLIC_BASE_URL=https://your-kaggle-bridge.vercel.app
