[project]
name = "kaggle-chatgpt-bridge"
version = "0.2.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.128,<1",
  "kaggle>=2.2,<3",
]

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.vercel]
entrypoint = "app:app"
